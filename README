Because later on there will be:

Monthly goals

Electricity usage records

So let's prepare the structure in advance.

data structure:
password (user password)
monthly_target (monthly energy target)
energy_records (daily energy records)

users = {
    username:{
        password,
        monthly_target,
        energy_records
    }
}

The front-end now needs to display:

Total Electricity Consumption This Month
Target
Completion Progress

Therefore, a new API is needed:
GET /energy-summary



The first step is to create an API route and confirm that the interface exists.

Let /energy-summary receive the username.

GET requests generally use a query parameter:

/energy-summary?username=alex



The next step is to:

Verify that the user exists.



If the user does not exist:

return 404

and,Basic API security checks

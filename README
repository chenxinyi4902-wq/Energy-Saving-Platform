Core needs:
Differentiate the current points_earned (calculated monthly) from the total points balance that is truly spendable.
A total_points field needs to be added to the user data as the available points balance.

Summary of major changes:

Added a new total_points field in user data as the truly redeemable points balance
/energy-summary still returns points_earned (points calculated monthly)
/get-user-info now returns total_points

All save operations will save total_points together with data.json

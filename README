The user might enter a non-existent username, so verify the user's existence first.

Add automatic date retrieval by the system.

Already implemented:

Create add-energy API
Receive energy
Verify user
Automatic date retrieval by the system

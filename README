Create a user data structure and add 
users = {}
to store user information in the future. 
The server can still function normally.

Add more test users.

This is because the leaderboard and statistics will require multiple users later.
users = {
    "alex": {
        "password": "123456"
    },
    "lisa": {
        "password": "abcdef"
    },
    "tom": {
        "password": "111111"
    }
}
